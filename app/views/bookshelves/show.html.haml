%h1= @bookshelf.name
.row
  - BookOnBookshelf.user_books.each do |book|
    .row
      .large-4.columns
        = image_tag book.cover_url, :width => '100%' if book.cover.present?
      .large-8.columns
        %h2= book.title
        %br/
        %span= book.author
